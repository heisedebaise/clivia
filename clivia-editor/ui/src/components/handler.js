const data = {};

const setAnnotation = (annotation) => data.annotation = annotation;

const annotation = () => {
    // if (data.annotation)
    //     data.annotation();
};

export {
    setAnnotation,
    annotation,
};